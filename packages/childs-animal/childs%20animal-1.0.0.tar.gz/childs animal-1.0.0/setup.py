import setuptools

setuptools.setup(name="childs animal",
                 version="1.0.0",
                 long_description="childs animal long discriotion",
                 packages=setuptools.find_packages(exclude=['env']))
                 