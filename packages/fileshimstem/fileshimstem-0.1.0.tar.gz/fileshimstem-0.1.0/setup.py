#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['fileshimstem']

package_data = \
{'': ['*']}

install_requires = \
['fastapi', 'uvicorn[standard]', 'GitPython']

extras_require = \
{'dev': ['pytest', 'pylint', 'mypy', 'flit']}

entry_points = \
{'console_scripts': ['fileshimstem = fileshimstem.__main__:run']}

setup(name='fileshimstem',
      version='0.1.0',
      description='fileshimstem - a terrible way to expose a filesystem over the network',
      author=None,
      author_email='James Hodgkinson <james@terminaloutcomes.com>',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
