#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['git_project_status']

package_data = \
{'': ['*']}

install_requires = \
['GitPython', 'loguru']

extras_require = \
{'dev': ['pytest', 'pylint', 'mypy', 'flit']}

entry_points = \
{'console_scripts': ['git_project_status = git_project_status.__main__:cli']}

setup(name='git_project_status',
      version='0.0.11',
      description='Scans the local directory for sub-directories that have',
      author=None,
      author_email='James Hodgkinson <james@terminaloutcomes.com>',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
