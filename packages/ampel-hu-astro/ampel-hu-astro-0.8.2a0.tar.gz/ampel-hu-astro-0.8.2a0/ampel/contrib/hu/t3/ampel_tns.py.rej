diff a/ampel/contrib/hu/t3/ampel_tns.py b/ampel/contrib/hu/t3/ampel_tns.py	(rejected hunks)
@@ -64,8 +64,8 @@ class TNSSession(BaseUrlSession):
             )).status_code != 429:
                 return response
             # back off according to rate-limit headers (see https://www.wis-tns.org/content/tns-newsfeed#comment-wrapper-26286)
-            delay = response.headers["x-cone-rate-limit-reset" if response.url.endswith('search') else "x-rate-limit-reset"]
-            time.sleep(int(delay))
+            delay = int(response.headers["x-cone-rate-limit-reset" if response.url.endswith('search') else "x-rate-limit-reset"])
+            time.sleep(max(delay, 1))
             
 
 
