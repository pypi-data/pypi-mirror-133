#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['fake_user_agent']

package_data = \
{'': ['*']}

install_requires = \
['lxml', 'aiohttp']

entry_points = \
{'console_scripts': ['fakeua = fake_user_agent.main:get_input']}

setup(name='fake_user_agent',
      version='0.0.14',
      description='Fake useragent randomly generates a useragent for fetching a webpage without a browser.',
      author='Kate Wang',
      author_email='kate.wang2018@gmail.com',
      url='https://github.com/KateWang2016/fake_user_agent',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
