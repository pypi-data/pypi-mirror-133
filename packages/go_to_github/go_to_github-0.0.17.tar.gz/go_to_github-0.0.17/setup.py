#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['go_to_github']

package_data = \
{'': ['*']}

install_requires = \
['lxml', 'fake_user_agent', 'requests']

entry_points = \
{'console_scripts': ['gotogithub = go_to_github.main:main']}

setup(name='go_to_github',
      version='0.0.17',
      description='`go_to_github` is a tool to test whether `github.com` is reachable right now. If not, it will work it out automatically.',
      author='Kate Wang',
      author_email='kate.wang2018@gmail.com',
      url='https://github.com/KateWang2016/go_to_github',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
