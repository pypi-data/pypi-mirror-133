#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['fail2ban_importer']

package_data = \
{'': ['*']}

install_requires = \
['boto3>=1.17.53', 'requests>=2.26.0', 'schedule>=1.1.0']

extras_require = \
{'dev': ['pylint', 'pytest', 'mypy', 'boto3-stubs', 'flit', 'black']}

entry_points = \
{'console_scripts': ['fail2ban_importer = fail2ban_importer.__main__:cli']}

setup(name='fail2ban_importer',
      version='0.0.4',
      description='Downloads JSON-encoded lists from s3 or HTTPS endpoints and bans them',
      author=None,
      author_email='James Hodgkinson <james@terminaloutcomes.com>',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
